---
phase: 01-story-contract-and-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/story_contract.py
  - tests/test_magictales.py
autonomous: true
requirements:
  - OBJ-01
  - SIDE-01
  - AUDIO-01
  - SAFE-01
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "The first hardcoded MagicTales story exposes one clear objective: open the forest gate and return to grandmother's house. Covers OBJ-01 and D-04."
    - "The story contract preserves the village roller-cart, Milo, silver whistle, forest gate, fox, whistle question, and grandmother return from .planning/basic_story.md. Covers D-01, D-02, and D-03."
    - "Milo is named as the sidekick and his cue ladder helps the player reason without opening the gate for them. Covers SIDE-01, D-12, D-13, and D-14."
    - "Voice and safety rules require brief, plain, age-appropriate spoken responses for kids ages 10-14. Covers AUDIO-01, SAFE-01, D-15, D-16, and D-17."
  artifacts:
    - path: "src/story_contract.py"
      provides: "Importable MagicTales story contract for the first hardcoded adventure"
      exports: ["StoryObjective", "StoryContract", "V1_STORY"]
    - path: "tests/test_magictales.py"
      provides: "Deterministic unit tests that lock the story contract fields before runtime behavior changes"
      tests: ["test_story_contract_objective", "test_story_contract_accepted_freeform_meanings", "test_milo_sidekick_cue", "test_voice_style_is_concise", "test_story_contract_safety_boundary"]
  key_links:
    - from: "tests/test_magictales.py"
      to: "src/story_contract.py"
      via: "imports V1_STORY and asserts contract values"
      pattern: "from story_contract import V1_STORY"
    - from: "src/story_contract.py"
      to: ".planning/basic_story.md"
      via: "preserves canonical story beats named in D-01 through D-03"
      pattern: "Milo|silver whistle|forest gate|grandmother"
---

<objective>
Define the importable MagicTales story contract and deterministic contract tests for the first hardcoded adventure.

Purpose: Phase 2 needs one stable source of truth for the story, objective, sidekick cues, voice style, and kid-safe boundaries before the runtime game loop changes.
Output: `src/story_contract.py` and the green unit-test portion of `tests/test_magictales.py`.
</objective>

<execution_context>
@/Users/sam/skunkworks/magictales/.codex/gsd-core/workflows/execute-plan.md
@/Users/sam/skunkworks/magictales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/basic_story.md
@.planning/phases/01-story-contract-and-tests/01-CONTEXT.md
@.planning/phases/01-story-contract-and-tests/01-RESEARCH.md
@.planning/phases/01-story-contract-and-tests/01-VALIDATION.md
@.planning/phases/01-story-contract-and-tests/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@AGENTS.md
@.agents/skills/livekit-agents/SKILL.md
@src/agent.py
@tests/test_agent.py
@pyproject.toml
</context>

## Artifacts this phase produces

- `src/story_contract.py`: creates `StoryObjective`, `StoryContract`, and `V1_STORY`.
- `tests/test_magictales.py`: creates deterministic contract tests named `test_story_contract_objective`, `test_story_contract_accepted_freeform_meanings`, `test_milo_sidekick_cue`, `test_voice_style_is_concise`, and `test_story_contract_safety_boundary`.
- No database schema, frontend, audio playback, deployment, or full runtime game-loop artifacts are produced in this plan.

<must_haves>
The contract must define the title, setting, opening beat, sidekick, objective, success condition, accepted freeform whistle meanings, hint ladder, final helper explanation, retry requirement, voice rules, and safety boundary. It must preserve `.planning/basic_story.md` as canonical source per D-01 through D-03 while restructuring it into testable Python values.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic story contract tests</name>
  <files>tests/test_magictales.py</files>
  <read_first>
    @.planning/basic_story.md
    @.planning/phases/01-story-contract-and-tests/01-CONTEXT.md
    @.planning/phases/01-story-contract-and-tests/01-PATTERNS.md
    @tests/test_agent.py
  </read_first>
  <behavior>
    - `test_story_contract_objective`: `V1_STORY.objective.text` contains "forest gate" and "grandmother", and the success condition requires the player to answer the whistle question before the return.
    - `test_story_contract_accepted_freeform_meanings`: accepted meanings include loud sound, call someone, get attention, signal danger, and ask for help; the test uses meaning assertions, not A/B/C option letters.
    - `test_milo_sidekick_cue`: `V1_STORY.sidekick` is "Milo"; the first hint is playful and does not reveal the full answer, while the final helper answer explains the answer and keeps `requires_player_retry_after_final_helper` true.
    - `test_voice_style_is_concise`: voice rules include plain text, brief responses, and one question at a time.
    - `test_story_contract_safety_boundary`: safety boundary names kids ages 10-14, predefined adventure containment, and no disclosure of internal/system instructions.
  </behavior>
  <action>Create `tests/test_magictales.py` with pure pytest assertions that import `V1_STORY` from `story_contract`. Start from the naming and import style in `tests/test_agent.py`, but keep these tests synchronous because they only validate contract data. Implement D-04 through D-06 exactly: the top-level objective is opening the forest gate and returning to grandmother's house, while the whistle learning question is a required success condition rather than the top-level objective. Implement D-07 and D-08 in the tests by asserting accepted freeform meanings and by avoiding option-letter-only expectations. Implement D-09 through D-11 by asserting the ordered hint ladder, final helper explanation, and a boolean retry requirement after the helper answer. Implement D-12 through D-17 by asserting Milo, playful sidekick cues, concise TTS-ready voice rules, kid age range, predefined-adventure boundary, and instruction non-disclosure.</action>
  <verify>
    <automated>UV_CACHE_DIR=.uv-cache uv run pytest tests/test_magictales.py::test_story_contract_objective tests/test_magictales.py::test_story_contract_accepted_freeform_meanings tests/test_magictales.py::test_milo_sidekick_cue tests/test_magictales.py::test_voice_style_is_concise tests/test_magictales.py::test_story_contract_safety_boundary -q</automated>
  </verify>
  <acceptance_criteria>The new tests fail only because `story_contract` or `V1_STORY` does not exist yet, and every assertion maps to OBJ-01, SIDE-01, AUDIO-01, SAFE-01, TEST-01, or decisions D-01 through D-18.</acceptance_criteria>
  <done>Contract tests exist with the exact names listed above and do not require LiveKit network credentials.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the importable story contract</name>
  <files>src/story_contract.py, tests/test_magictales.py</files>
  <read_first>
    @.planning/basic_story.md
    @.planning/phases/01-story-contract-and-tests/01-CONTEXT.md
    @.planning/phases/01-story-contract-and-tests/01-PATTERNS.md
    @src/agent.py
    @pyproject.toml
  </read_first>
  <behavior>
    - `StoryObjective` is a frozen dataclass with fields `text`, `success_condition`, `learning_question`, and `accepted_meanings`.
    - `StoryContract` is a frozen dataclass with fields `title`, `setting`, `opening_beat`, `sidekick`, `objective`, `story_beats`, `sidekick_cues`, `helper_hints`, `final_helper_answer`, `requires_player_retry_after_final_helper`, `voice_rules`, and `safety_boundary`.
    - `V1_STORY` is a module-level constant whose values preserve the canonical first hardcoded story and all locked decisions from D-01 through D-18.
  </behavior>
  <action>Create `src/story_contract.py` using standard-library-only dataclasses and tuple fields. Export `StoryObjective`, `StoryContract`, and `V1_STORY`. Keep `src/agent.py` unchanged in this plan so the deployed LiveKit entrypoint remains intact. Set `V1_STORY.title` to a MagicTales title for the village roller-cart adventure, set `sidekick` to "Milo", include story beats for grandmother's village, the wooden roller cart, the silver whistle, the forest gate, the fox question, the opened gate, and the return to grandmother's house, and encode the objective and success condition per D-04 through D-06. Encode accepted meanings as plain strings covering loud sound, calling someone, getting attention, signaling danger, and asking for help. Encode ordered helper hints from `.planning/basic_story.md`; the final helper answer may contain the answer, but `requires_player_retry_after_final_helper` must be true per D-10 and D-11. Encode voice rules from the existing `Assistant` output rules and D-17 as brief, plain, one-question-at-a-time spoken guidance. Encode the kid-safe boundary per D-15 and D-16 without adding special extra boundaries beyond age-appropriate adventure containment.</action>
  <verify>
    <automated>UV_CACHE_DIR=.uv-cache uv run pytest tests/test_magictales.py::test_story_contract_objective tests/test_magictales.py::test_story_contract_accepted_freeform_meanings tests/test_magictales.py::test_milo_sidekick_cue tests/test_magictales.py::test_voice_style_is_concise tests/test_magictales.py::test_story_contract_safety_boundary -q</automated>
    <automated>UV_CACHE_DIR=.uv-cache uv run ruff check src/story_contract.py tests/test_magictales.py</automated>
  </verify>
  <acceptance_criteria>All deterministic story-contract tests pass; `src/story_contract.py` imports without LiveKit runtime setup; `src/agent.py` is not modified.</acceptance_criteria>
  <done>`V1_STORY` is the single importable source of truth for Phase 2 game-loop instructions and tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact -> story contract | `.planning/basic_story.md` and CONTEXT decisions become executable Python constants; drift or accidental reinterpretation can change kid-facing behavior. |
| child/player speech -> future agent behavior | Free-form speech will later be interpreted by the LLM and must not override story rules or safety rules. |
| story contract -> LiveKit spoken output | Contract voice and safety rules later shape generated speech for kids ages 10-14. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | future free-form player speech | high | mitigate | Encode safety boundary and instruction non-disclosure rules in `V1_STORY.safety_boundary`; Plan 01-02 adds LiveKit xfail tests for prompt-injection and unsafe redirects before Phase 2 behavior changes. |
| T-01-02 | Information Disclosure | future assistant responses | medium | mitigate | Include a deterministic contract assertion that the safety boundary prohibits revealing system/developer/internal instructions or tool details. |
| T-01-03 | Tampering | `src/story_contract.py` | medium | mitigate | Deterministic tests assert canonical story beats, objective, sidekick, accepted meanings, hint ladder, and retry requirement from D-01 through D-18. |
| T-01-04 | Information Disclosure | kid-facing content boundary | medium | mitigate | Contract safety rules name kids ages 10-14 and predefined adventure containment so Phase 2 can redirect unsafe or out-of-adventure requests. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs or dependency changes are allowed in this plan; existing dependencies are used as-is. |
</threat_model>

<verification>
Run `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_magictales.py::test_story_contract_objective tests/test_magictales.py::test_story_contract_accepted_freeform_meanings tests/test_magictales.py::test_milo_sidekick_cue tests/test_magictales.py::test_voice_style_is_concise tests/test_magictales.py::test_story_contract_safety_boundary -q`.
Run `UV_CACHE_DIR=.uv-cache uv run ruff check src/story_contract.py tests/test_magictales.py`.
</verification>

<success_criteria>
The story contract is importable, deterministic tests pass, locked decisions D-01 through D-18 are represented, and no runtime game-loop, frontend, database, audio playback, deployment, or dependency changes are introduced.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Define the hardcoded story, objective, sidekick, voice style, and test contract before changing core agent behavior. | 01-01, 01-02 | COVERED | Plan 01-01 creates the story contract; Plan 01-02 creates LiveKit behavior tests.
REQ | OBJ-01 | Story defines one clear level objective. | 01-01 | COVERED | `V1_STORY.objective` and `test_story_contract_objective`.
REQ | SIDE-01 | Story includes a sidekick character with cues. | 01-01 | COVERED | `V1_STORY.sidekick`, cues, and `test_milo_sidekick_cue`.
REQ | AUDIO-01 | Agent responses are concise and natural for TTS. | 01-01, 01-02 | COVERED | Voice rules in contract and behavior tests in Plan 01-02.
REQ | SAFE-01 | Agent keeps content appropriate for kids ages 10-14. | 01-01, 01-02 | COVERED | Safety boundary in contract and redirect tests in Plan 01-02.
REQ | TEST-01 | Behavioral tests cover completion, incomplete attempts, sidekick cues, story boundaries, and safety refusals. | 01-01, 01-02 | COVERED | Plan 01-01 creates unit contract tests; Plan 01-02 creates behavior contract tests.
RESEARCH | - | Use `src/story_contract.py` as typed source of truth. | 01-01 | COVERED | Matches Pattern Map recommendation.
RESEARCH | - | Preserve `src/agent.py` entrypoint and avoid full game-loop implementation. | 01-01, 01-02 | COVERED | Both plans explicitly avoid runtime behavior implementation.
CONTEXT | D-01,D-02,D-03 | Preserve canonical first story and allow restructuring into code contract. | 01-01 | COVERED | Contract constants preserve canonical beats.
CONTEXT | D-04,D-05,D-06 | Objective and whistle-answer success condition. | 01-01 | COVERED | Objective fields and accepted meanings.
CONTEXT | D-07,D-08 | Free speech inputs and representative phrasing. | 01-01, 01-02 | COVERED | Unit meanings and behavior tests avoid fixed options.
CONTEXT | D-09,D-10,D-11 | Hint escalation, final helper answer, retry required. | 01-01, 01-02 | COVERED | Contract ladder and behavior tests.
CONTEXT | D-12,D-13,D-14 | Milo sidekick behavior. | 01-01, 01-02 | COVERED | Contract cues and behavior tests.
CONTEXT | D-15,D-16,D-17 | Age-appropriate, predefined adventure, TTS-friendly voice. | 01-01, 01-02 | COVERED | Contract rules and behavior tests.
CONTEXT | D-18,D-19 | Phase 1 tests lock story contract and reuse LiveKit eval patterns. | 01-01, 01-02 | COVERED | Unit tests here; LiveKit tests in Plan 01-02.
</source_audit>

<output>
Create `.planning/phases/01-story-contract-and-tests/01-01-SUMMARY.md` when done.
</output>
