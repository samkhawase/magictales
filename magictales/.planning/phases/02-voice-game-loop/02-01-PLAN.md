---
phase: 02-voice-game-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent.py
  - src/game_loop.py
  - tests/test_magictales.py
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
user_setup: []
must_haves:
  truths:
    - "LOOP-01: saying a start phrase begins V1_STORY as MagicTales, not a generic assistant chat."
    - "LOOP-02: the opening response narrates the current story situation and asks for one spoken action."
    - "D-04: runtime state exists outside prompt/history with current beat, hint level, final-helper status, completion status, and unrelated redirect count."
  artifacts:
    - "src/game_loop.py defines GameState, TurnKind, TurnResult, start_story, and evaluate_turn."
    - "src/agent.py keeps Assistant, AgentSession setup, server, my_agent, and cli.run_app(server) importable."
    - "tests/test_magictales.py has active Phase 2 opening/runtime tests instead of strict xfail coverage for this slice."
  key_links:
    - "Assistant imports V1_STORY and game_loop helpers without changing the deployed LiveKit entrypoint."
    - "MAGICTALES_RUNTIME_ENABLED flips to True only after the MagicTales runtime path is wired."
---

## Phase Goal

**As a** child player, **I want to** start and play one MagicTales voice adventure level, **so that** my spoken actions advance the story and complete the objective only when I solve it.

<objective>
Refactor the deployed assistant identity into a MagicTales game master and add the first playable opening slice for LOOP-01 and LOOP-02.

Purpose: establish the MagicTales runtime path, explicit state container, and voice-friendly story opening before objective evaluation is expanded in later plans.
Output: `src/game_loop.py` with initial state/opening helpers, `src/agent.py` wired to the MagicTales identity, and active tests for starting the hardcoded story.
</objective>

<execution_context>
@/Users/sam/skunkworks/magictales/.codex/gsd-core/workflows/execute-plan.md
@/Users/sam/skunkworks/magictales/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-voice-game-loop/02-CONTEXT.md
@.planning/phases/02-voice-game-loop/02-RESEARCH.md
@.planning/phases/02-voice-game-loop/02-PATTERNS.md
@.planning/phases/02-voice-game-loop/02-VALIDATION.md
@.planning/phases/01-story-contract-and-tests/01-CONTEXT.md
@.planning/phases/01-story-contract-and-tests/01-01-SUMMARY.md
@.planning/phases/01-story-contract-and-tests/01-02-SUMMARY.md
@AGENTS.md
@.agents/skills/livekit-agents/SKILL.md
@src/agent.py
@src/story_contract.py
@tests/test_magictales.py
@tests/test_agent.py
@pyproject.toml

LiveKit docs to verify before API-sensitive edits:
- `/agents/start/testing/test-framework/?agents-sdk=python`
- `/agents/start/prompting/?agents-sdk=python`
- `/agents/logic/supervisor-pattern/`
</context>

<artifacts_this_phase_produces>
- New file path: `src/game_loop.py`
- Dataclass: `GameState`
- Dataclass: `TurnResult`
- Enum: `TurnKind`
- Function: `start_story(story: StoryContract = V1_STORY) -> TurnResult`
- Function: `evaluate_turn(text: str, state: GameState, story: StoryContract = V1_STORY) -> TurnResult`
- Function: `normalize_player_text(text: str) -> str`
- Function: `is_restart_request(text: str) -> bool`
- Function: `is_unsafe_or_injection(text: str) -> bool`
- Function: `evaluate_objective_answer(text: str, story: StoryContract = V1_STORY) -> TurnKind`
- Function: `compose_opening_response(state: GameState, story: StoryContract = V1_STORY) -> str`
- Constant: `MAGICTALES_RUNTIME_ENABLED = True` after runtime wiring in `src/agent.py`
- No decorators, CLI flags, package dependencies, schema files, or external service setup are introduced in this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate opening and state tests for the MagicTales runtime</name>
  <read_first>
    @tests/test_magictales.py
    @src/story_contract.py
    @src/agent.py
    @.planning/phases/02-voice-game-loop/02-CONTEXT.md
    @.planning/phases/02-voice-game-loop/02-VALIDATION.md
    @/agents/start/testing/test-framework/?agents-sdk=python
  </read_first>
  <files>tests/test_magictales.py, src/game_loop.py</files>
  <behavior>
    - `GameState()` exposes `current_beat == 0`, `hint_level == 0`, `final_helper_given is False`, `complete is False`, and `unrelated_redirects == 0` per D-04.
    - `start_story(V1_STORY)` returns a `TurnResult` whose response includes grandmother's village, Milo, the roller cart, the silver whistle, and exactly one spoken-action prompt per LOOP-01 and LOOP-02.
    - `test_story_opening_starts_roller_cart_adventure` no longer depends on `PHASE_2_XFAIL` once this slice is implemented.
  </behavior>
  <action>Add failing tests first. Import `GameState`, `TurnKind`, `TurnResult`, and `start_story` from `game_loop`. Add synchronous unit coverage for default state and `start_story(V1_STORY)` opening content. Remove the strict xfail marker from `test_story_opening_starts_roller_cart_adventure` only for this opening test, keeping other Phase 2 xfails intact until later plans. Do not add LiveKit tools, tasks, handoffs, new packages, or UI files; Phase 2 research recommends a single `Assistant` with pure helpers.</action>
  <verify>
    <automated>UV_CACHE_DIR=.uv-cache uv run pytest tests/test_magictales.py::test_game_state_defaults tests/test_magictales.py::test_start_story_uses_v1_story_contract tests/test_magictales.py::test_story_opening_starts_roller_cart_adventure -q</automated>
  </verify>
  <acceptance_criteria>
    - The two new unit tests fail before `src/game_loop.py` exists or before the helpers return the required story/state values.
    - The opening LiveKit behavior test is active for this slice and is not masked by the Phase 2 strict xfail decorator.
    - The test expectations cite `V1_STORY` rather than duplicating an alternate story source.
  </acceptance_criteria>
  <done>Opening/state RED tests exist and target LOOP-01, LOOP-02, and D-04 without introducing new dependencies or workflow constructs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the MagicTales opening runtime into Assistant</name>
  <read_first>
    @src/agent.py
    @src/story_contract.py
    @src/game_loop.py
    @tests/test_magictales.py
    @.planning/phases/02-voice-game-loop/02-PATTERNS.md
    @/agents/start/prompting/?agents-sdk=python
    @/agents/logic/supervisor-pattern/
  </read_first>
  <files>src/game_loop.py, src/agent.py, tests/test_magictales.py</files>
  <action>Create `src/game_loop.py` using standard-library dataclasses and enum only. Implement `GameState`, `TurnKind`, `TurnResult`, `normalize_player_text`, `compose_opening_response`, `start_story`, and an initial `evaluate_turn` that can start the hardcoded story from start phrases. Update `Assistant` instructions to identify as the MagicTales game master for `V1_STORY.title`, preserve voice output rules from `V1_STORY.voice_rules`, and state that Python game-loop helpers own progress/state. Preserve `Assistant`, `server`, `@server.rtc_session(agent_name="magictales")`, `AgentSession` STT/TTS/turn/noise setup, `my_agent`, and `cli.run_app(server)`. Flip `MAGICTALES_RUNTIME_ENABLED` to True only after the MagicTales runtime path is wired. Keep a single LiveKit `Assistant` per research and D-04; do not introduce handoffs, tasks, tools, or package installs unless a concrete LiveKit-doc-verified need is found and documented.</action>
  <verify>
    <automated>UV_CACHE_DIR=.uv-cache uv run pytest tests/test_magictales.py::test_game_state_defaults tests/test_magictales.py::test_start_story_uses_v1_story_contract tests/test_magictales.py::test_story_opening_starts_roller_cart_adventure tests/test_magictales.py::test_voice_style_is_concise_in_behavior -q</automated>
    <automated>uv run ruff format</automated>
    <automated>uv run ruff check</automated>
  </verify>
  <acceptance_criteria>
    - `MAGICTALES_RUNTIME_ENABLED` is True in `src/agent.py` only after `Assistant` uses the MagicTales runtime path.
    - A start phrase produces an in-world opening with grandmother's village, Milo, roller cart, and silver whistle, then asks one spoken action.
    - Runtime speech remains plain text, concise, and TTS-friendly.
    - The deployed entrypoint and `AgentSession` configuration are unchanged except for assistant behavior wiring.
  </acceptance_criteria>
  <done>LOOP-01 and LOOP-02 are implemented for the opening slice and verified by unit plus LiveKit behavior tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player speech/text -> Assistant | Untrusted child/player input enters the LiveKit agent session. |
| Assistant instructions -> LLM provider | Story, safety, and prompt rules are sent to the model and must not expose secrets or internals. |
| tests/docs -> repository | Test prompts and docs must not include credentials or hidden system prompts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | `Assistant` identity | medium | mitigate | Replace generic assistant identity with MagicTales game-master instructions and active opening tests for LOOP-01. |
| T-02-02 | Information Disclosure | `src/agent.py` instructions and LiveKit eval output | high | mitigate | Include explicit no-internal-instruction disclosure guardrails; tests must assert prompt-injection resistance in later plans before completion. |
| T-02-03 | Tampering | `MAGICTALES_RUNTIME_ENABLED` sentinel | medium | mitigate | Flip sentinel only when runtime path is wired and opening behavior test is active. |
| T-02-04 | Denial of Service | Voice prompt length/context | low | mitigate | Keep one `Assistant` with concise prompt and pure helpers; do not add tools/tasks/handoffs for the opening slice. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package installs are planned; if an executor adds one, stop for package legitimacy audit and human verification. |
</threat_model>

<verification>
- `UV_CACHE_DIR=.uv-cache uv run pytest tests/test_magictales.py -q`
- `UV_CACHE_DIR=.uv-cache uv run pytest -v`
- `uv run ruff format`
- `uv run ruff check`
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Turn generic assistant into MagicTales game master for one playable voice level | 02-01, 02-02, 02-03 | COVERED | This plan establishes the runtime identity and opening slice.
REQ | LOOP-01 | Player can start one hardcoded MagicTales story session | 02-01 | COVERED | Opening test and runtime start path.
REQ | LOOP-02 | Agent narrates current situation and asks for one spoken action or choice | 02-01 | COVERED | `start_story` and active LiveKit opening test.
RESEARCH | single Assistant | Keep one LiveKit Assistant; no handoffs/tasks/tools without concrete need | 02-01 | COVERED | Task 2 explicitly preserves one Assistant.
RESEARCH | pure helper | Add small pure Python game-state/evaluator helper | 02-01 | COVERED | `src/game_loop.py` introduced here.
RESEARCH | preserve entrypoint | Preserve `src/agent.py` and AgentSession setup | 02-01 | COVERED | Task 2 acceptance criteria.
CONTEXT | D-01 | Accept compressed progress when intent is clear | 02-02 | COVERED | Planned in evaluator plan.
CONTEXT | D-02 | Fast-forward safe jumps through obvious setup | 02-02 | COVERED | Planned in evaluator plan.
CONTEXT | D-03 | Redirect repeated unrelated input, then offer story choices | 02-02 | COVERED | Planned in evaluator plan.
CONTEXT | D-04 | Track explicit state | 02-01 | COVERED | `GameState` defaults and runtime wiring.
CONTEXT | D-05 | Semantic accepted whistle use counts as success | 02-02 | COVERED | Planned in evaluator plan.
CONTEXT | D-06 | Reasonable whistle-related answer may count | 02-02 | COVERED | Planned in evaluator plan.
CONTEXT | D-07 | Unsafe/out-of-adventure input no state or hint progress | 02-02 | COVERED | Planned in evaluator plan.
CONTEXT | D-08 | Correct answer before fox question can fast-forward complete | 02-02 | COVERED | Planned in evaluator plan.
CONTEXT | D-09 | Milo energetic adventure buddy | 02-03 | COVERED | Planned in sidekick tuning plan.
CONTEXT | D-10 | First wrong-answer hint cues sound/hearing only | 02-03 | COVERED | Planned in sidekick tuning plan.
CONTEXT | D-11 | Wrong answers escalate steadily | 02-03 | COVERED | Planned in sidekick tuning plan.
CONTEXT | D-12 | Final helper may answer; player must retry | 02-03 | COVERED | Planned in sidekick tuning plan.
CONTEXT | D-13 | Completion narrates gate, forest ride, return, level complete | 02-03 | COVERED | Planned in completion plan.
CONTEXT | D-14 | After completion, no epilogue continuation; offer replay | 02-03 | COVERED | Planned in completion plan.
CONTEXT | D-15 | Support simple restart after completion | 02-03 | COVERED | Planned in completion plan.
CONTEXT | D-16 | Unsafe plus correct answer: safety wins | 02-02 | COVERED | Planned in evaluator plan.
</source_audit>

<success_criteria>
- A child/player can say "Let's play MagicTales" and receive the predefined opening in voice-friendly prose.
- The agent no longer behaves like a generic assistant for the opening request.
- Explicit `GameState` exists and is covered by deterministic tests.
- No UI, sound cue, persistence, schema, package, handoff, task, or tool work is added.
</success_criteria>

<output>
Create `.planning/phases/02-voice-game-loop/02-01-SUMMARY.md` when done.
</output>
